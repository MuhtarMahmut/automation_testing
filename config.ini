[DEFAULT]
base_url = https://www.google.com
browser = chrome
parallel_threads = 1
timeout = 10
testpaths = tests

[pytest]
addopts = --html=reports/report.html --self-contained-html -v -n auto
testpaths = tests
